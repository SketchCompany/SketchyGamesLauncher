import SettingsShell from "../components/settings/SettingsShell.jsx"

/** Einstellungen — Discord-artige Oberfläche (Kategorie-Leiste + Inhalt), siehe SettingsShell. */
export default function Settings() {
	return <SettingsShell defaultSection="benachrichtigungen" />
}
